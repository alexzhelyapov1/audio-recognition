# AI Prompts Log

```
Ознакомься с ТЗ в файле [имя файла] и правилами работы в GEMINI.md. Начни выполнение проекта, строго следуя Phase 1 из Development Pipeline. Создай структуру `.memory-bank` и сгенерируй все необходимые схемы и документы. Жду отчет по Phase 1.
```

```
Отличная работа! Архитектура, схемы и БД спроектированы идеально и полностью соответствуют ТЗ. 

Перед тем как перейти к Phase 2 (Implementation), учти 3 небольших уточнения:
1. В ТЗ явно указано требование "регистрация". Добавь в `api_endpoints.md` и в систему простой открытый роут `GET/POST /register` для самостоятельной регистрации операторов (чтобы не зависеть только от админа).
2. Запомни для Phase 3: поскольку ты используешь `pydub` для конвертации аудио, в финальном `README.md` обязательно нужно будет указать `ffmpeg` как системную зависимость для проверяющего эксперта.
3. В логике парсера (которую ты будешь писать в Phase 2) используй строгие Regex выражения для поиска 5 конкретных команд ("Зарегистрировать", "Начать обработку", "Отменить обработку", "Отменить регистрацию", "Завершить обработку") и извлечения идентификаторов (строго 8 цифр ИЛИ цифробуквенные сочетания).

Обнови `.memory-bank` с учетом пункта 1 (добавь роут регистрации).
После этого официально переходи к **Phase 2: Implementation**. 
Начни с создания базовой структуры Flask приложения (`app.py` или `__init__.py`), настройки SQLite базы данных (`models.py`) и базовых шаблонов. Пиши код небольшими логическими блоками. Начни с моделей БД и аутентификации.
```

```
Код-ревью пройдено успешно! Структура Flask-приложения, аутентификация и модели БД написаны без ошибок и отлично соответствуют MVP.

Можешь продолжать выполнение по плану. Переходи к следующим шагам:
1. Напиши клиентскую часть (`app/static/js/recorder.js`) с использованием `MediaRecorder API` для записи звука и отправки его через `fetch` (POST) на сервер.
2. Создай модуль `app/recognition/vosk_engine.py` (или аналогичный), где будет реализована конвертация аудио через `pydub` в нужный для VOSK формат (WAV Mono 16kHz) и само распознавание.
   * Важно: укажи в комментариях кода или логах, где должна лежать папка с языковой моделью VOSK (например, в корне `model/`), чтобы приложение не падало при запуске.
3. В этом же или отдельном модуле реализуй строгий парсер (Regex), который извлечет из текста VOSK одну из 5 команд ("Зарегистрировать", "Начать обработку" и т.д.) и идентификатор (8 цифр или буквенно-цифровые).
4. Напиши роут `/record/upload` в `app/routes.py`, который свяжет всё это вместе, сохранит файл, добавит запись в БД и вернет JSON клиенту.

Продолжай писать код логическими блоками. Жду результат.
```

```
Код-ревью пройдено. Отличная работа! 

Особенно хвалю за реализацию `SpeechRecognizer._model` как переменной класса (это спасет оперативную память) и за аккуратную очистку временных `.webm` файлов. Регулярные выражения тоже выглядят надежно.

Приступай к завершающим шагам Phase 2:
1. Создай шаблон `history.html`. Реализуй там таблицу со списком записей и базовую форму фильтрации (например, по дате или типу команды). Убедись, что плеер аудио получает правильный путь: `url_for('static', filename='uploads/audio/' + record.audio_file_path)`.
2. Создай интерфейс и роуты администратора (например, `/admin/users`) для просмотра списка пользователей, их блокировки (`is_active = False`) и назначения ролей. Не забудь защитить эти роуты проверкой на то, что `current_user.role == 'Admin'`.
3. Интегрируй новые страницы в навигационное меню (`base.html`).

После выполнения этих шагов жду отчет, чтобы мы могли перейти к финальной Phase 3 (документация и подготовка к сдаче).
```

```
Код-ревью пройдено блестяще! Отличная работа. Особенно хвалю за правильный декоратор `@admin_required`, защиту от самоблокировки админа и аккуратную фильтрацию истории. Фаза 2 официально завершена, техническое решение работает.

Переходи к **Phase 3: Deliverables & Finalization**. 
Твоя задача сейчас — сгенерировать идеальную документацию. Помни, что проверяющий эксперт будет оценивать проект именно по ней.

Сделай следующее:
1. Создай `README.md` в корне проекта. Это инструкция для эксперта (оценивается в 20 баллов). Структура:
   - Краткое описание проекта и используемый стек (Flask, VOSK, SQLite).
   - Системные требования: **Критически важно** явно указать необходимость установки `ffmpeg` в ОС (дай краткие команды для Ubuntu `apt install ffmpeg` и ссылку для Windows).
   - Инструкция по установке: создание `venv`, `pip install -r requirements.txt`.
   - Инструкция по VOSK: дай четкую ссылку на скачивание легкой русской модели (например, `vosk-model-small-ru-0.22`), скажи, что ее нужно распаковать и переименовать папку в `model/` в корне проекта.
   - Запуск: `python main.py` и логин/пароль дефолтного администратора (`admin:admin123`).

2. Создай файл `USER_MANUAL.md`. В нем распиши пошаговый сценарий (с тест-кейсами), по которому студент будет записывать демонстрационный видеоролик. Опиши шаги: 
   - Вход под админом, создание оператора.
   - Вход под оператором, запись команды (например, "Зарегистрировать трубу P45345ИВ").
   - Демонстрация ручной корректировки.
   - Переход в историю и использование фильтров.

3. **САМОЕ ВАЖНОЕ (Критерий ТЗ)**: Обнови файл `.memory-bank/ai_prompts_log.md`. Запиши туда подробно все наши шаги и промпты по Фазе 2 и Фазе 3. По условиям олимпиады, если использование ИИ не задокументировано детально, работа аннулируется.

4. Проверь все файлы в `.memory-bank`, чтобы схемы (ERD, DFD) полностью соответствовали тому коду, который мы написали.

Жду финальные markdown файлы!
```

```
Нам необходимо подготовить финальный сводный документ для проверяющего эксперта, чтобы получить максимальный балл (35 баллов) за проектирование системы.

Твоя задача: на основе всех файлов из директории `.memory-bank` создай в корне проекта один большой Markdown-файл с названием `TECHNICAL_ARCHITECTURE.md` (весь текст внутри должен быть на русском языке). 

Этот файл должен строго соответствовать следующим критериям оценки из Технического Задания:

1. **Раздел 1: Схема решения и использование ИИ (Критерий 2.1 - 5 баллов)**
   - Вставь общую схему решения (DFD из system_architecture.md).
   - Создай подраздел "Использование ИИ", где укажи: "В проекте использовался ИИ-ассистент: [ВСТАВИТЬ НАЗВАНИЕ ИИ]".
   - Скопируй сюда краткий лог промптов из `ai_prompts_log.md` (какие задачи решались с помощью ИИ).
   - Оставь плейсхолдер `[ОПИСАНИЕ ЛИЧНОГО ВКЛАДА]`, чтобы я мог сам вписать свой личный вклад в архитектуру.

2. **Раздел 2: Описание предлагаемых технологий (Критерий 2.2 - 5 баллов)**
   - Перенеси таблицу и описание стека технологий из `system_architecture.md` (Flask, SQLite, VOSK, pydub, Vanilla JS). Объясни, почему выбрана каждая технология в контексте MVP.

3. **Раздел 3: Структура базы данных (Критерий 2.3 - 10 баллов)**
   - Вставь ERD-диаграмму в формате Mermaid (из `database_schema.md`).
   - Перенеси инфологическое и даталогическое описание: таблицы (User, Record), подробное описание полей, типов данных, связей (Primary/Foreign keys) и ключевых атрибутов.

4. **Раздел 4: Процессы, сценарии и потоки (Критерий 2.4 - 15 баллов)**
   - Вставь UML Use Case диаграмму в формате Mermaid (из `use_cases.md`).
   - Опиши информационные потоки (как аудио идет от браузера к VOSK и в БД).
   - Подробно распиши 4 основных пользовательских сценария (Запись, Корректировка, Поиск, Администрирование).

Сделай документ красиво оформленным, с правильной иерархией заголовков, чтобы его можно было легко экспортировать в PDF или читать напрямую в Git-репозитории.
```